<?php

class PopulateController extends ApiController {

    /**
     * Start parsing the file and putting it into
     * the mysql database for later use.
     *
     * @param $id
     */
    public function parseFile($id) {

    }

}